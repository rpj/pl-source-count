A source code line counter and defect estimation script in Perl, with support for most common source file types.

To add a file type, check out the $file_types variable declared at line 42. Always leave the first two zeros in the array (as shown by every other element), The last element is the the comment type(s) to look for with that type of file; see other entries for examples.